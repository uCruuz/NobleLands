{
  "name": "tribalwars-monorepo",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"BACK,FRONT\" --prefix-colors \"white,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
